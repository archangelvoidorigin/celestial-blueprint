# TASK T1.8.03 — Cross-tool skill-load test

- **Stage / Phase**: Stage 1 — Forge / Phase 1.8
- **Depends on**: T1.5.07
- **Produces**: evidence (run-and-verify)

## Context
The methodology skill must actually load and trigger in the tools the user uses. This test confirms `link_skill.sh` wired the skill correctly and that at least one agent tool recognises and uses it.

## Objective
Prove the `celestial-blueprint` skill loads in Claude Code and (if available) OpenCode, and that a planning request correctly runs the interrogation protocol.

## Instructions
1. Run the linker:
   ```bash
   bash /home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Blueprint/skill/celestial-blueprint/link_skill.sh
   ```
   Expected: prints `LINKED` or `EXISTS` for at least one target; no `FAIL`.

2. **Claude Code** — in a new Claude Code session, type:
   ```
   /skills list
   ```
   Expected: `celestial-blueprint` appears in the list.

3. Send a planning request:
   > "I want to plan a new CLI tool for managing notes. Use the celestial-blueprint skill."

   Expected: the agent selects a tier, then runs Step 1 (Identity & terrain) before planning.

4. **OpenCode** (if installed) — open a new session, type:
   > "Use the celestial-blueprint skill to plan a new database schema."

   Expected: same interrogation flow.

5. Confirm degradation: disconnect the backend and repeat the request. Expected: agent falls back to standalone mode and produces the markdown blueprint as a code block.

## Acceptance Criteria
- [ ] `link_skill.sh` reports at least one `LINKED` or `EXISTS`
- [ ] Claude Code `/skills list` includes `celestial-blueprint`
- [ ] A planning request triggers the BLANK SLATE interrogation (Step 1 questions)
- [ ] The skill produces a valid blueprint (copy-pasteable markdown conforming to `docs/FORMAT.md`)

## Verify Command
Manual (described above). Log results in `VERIFY.md` section 8.

## Done When
The skill is loaded, triggers correctly, and produces valid blueprint markdown in at least one agent tool.
