# TASK T1.8.01 — `VERIFY.md` E2E manual checklist

- **Stage / Phase**: Stage 1 — Forge / Phase 1.8
- **Depends on**: all of 1.1–1.7
- **Produces**: `BUILD_PLAN/STAGE_1_FORGE/PHASE_1.8_VERIFICATION/VERIFY.md`

## Context
After all Stage 1 tasks complete, a human needs to confirm the full stack works end-to-end before declaring Stage 1 done. `VERIFY.md` is that checklist — run it top to bottom. Each check has a command or action and an expected result. When all boxes are checked, Stage 1 is done.

## Objective
Create `VERIFY.md` with the complete E2E checklist for Stage 1.

## Instructions
1. Create `BUILD_PLAN/STAGE_1_FORGE/PHASE_1.8_VERIFICATION/VERIFY.md` with exactly:
   ```markdown
   ---
   title: "Stage 1 — E2E Verification Checklist"
   tags: [verify, stage-1, checklist]
   status: pending
   date_created: 2026-06-24
   ---

   # Stage 1 — E2E Verification Checklist

   Check every box. Commands run from the project root unless noted.

   ## 1. Setup
   - [ ] `make install` completes without error (backend venv + frontend node_modules)
   - [ ] `python -c "import fastapi, pydantic, yaml, sqlite3; print('ok')"` prints `ok`
   - [ ] `cd frontend && npm run build` completes without TypeScript errors

   ## 2. Tests
   - [ ] `cd backend && source .venv/bin/activate && pytest -q` → all tests pass (no failures)
   - [ ] Round-trip tests pass: `pytest tests/test_roundtrip.py -q`
   - [ ] Store tests pass: `pytest tests/test_store.py -q`
   - [ ] API tests pass: `pytest tests/test_api.py -q`

   ## 3. Backend smoke test
   - [ ] `fish dev.fish` (or `make backend` in one terminal, `make frontend` in another) starts without error
   - [ ] `curl http://localhost:8787/api/health` → `{"status":"ok","service":"celestial-blueprint"}`
   - [ ] `curl -X POST http://localhost:8787/api/blueprints -H "Content-Type: application/json" -d '{"name":"Smoke Test","tier":"small"}' | python -m json.tool | grep '"id"'` → prints a blueprint id

   ## 4. Frontend smoke test
   - [ ] Open `http://localhost:5173` — dark-themed "Blueprints" list page loads
   - [ ] Click "+ New", create a blueprint named "Smoke Test", tier "small" — navigates to canvas
   - [ ] Canvas shows the "Smoke Test" blueprint name in the header
   - [ ] Click "+ Node", add a text node "Hello", type "text" — node appears on canvas
   - [ ] Connect two nodes by dragging a handle — edge appears

   ## 5. Chronicle + file sync
   - [ ] In the ChroniclePanel, add a RAW entry "first note" — entry appears in the list
   - [ ] Check that `blueprints/bp_*.md` exists on disk with the blueprint's content
   - [ ] Open that file — it contains `<!-- node:text id=` (node anchor present)

   ## 6. Export / Import / Rebuild
   - [ ] `curl http://localhost:8787/api/blueprints/<id>/export` → returns markdown starting with `---`
   - [ ] Paste the markdown into `POST /api/blueprints/import` → returns the same blueprint
   - [ ] `POST /api/rebuild` → `{"imported": N}` where N ≥ 1

   ## 7. Recovery test
   > Follow T1.8.02 (delete DB → rebuild)

   ## 8. Skill load test
   > Follow T1.8.03 (load skill in Claude Code)

   ---

   **Stage 1 is DONE when all boxes above are checked.**
   Update `00_TASK_INDEX.md` status to "Complete" and start Stage 2 decomposition.
   ```

## Acceptance Criteria
- [ ] `VERIFY.md` exists with all 8 sections
- [ ] Every check has an action and expected result
- [ ] Includes pointers to T1.8.02 and T1.8.03 for the recovery and skill tests

## Verify Command
```bash
cd /home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Blueprint
test -f BUILD_PLAN/STAGE_1_FORGE/PHASE_1.8_VERIFICATION/VERIFY.md && grep -q "Stage 1 is DONE" BUILD_PLAN/STAGE_1_FORGE/PHASE_1.8_VERIFICATION/VERIFY.md && echo "verify-md ok"
```
Expected: prints `verify-md ok`

## Done When
`VERIFY.md` exists and is usable as the Stage 1 sign-off checklist.
