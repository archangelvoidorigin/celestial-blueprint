# TASK T1.5.05 — `SKILL.md` degradation modes + agent roster

- **Stage / Phase**: Stage 1 — Forge / Phase 1.5
- **Depends on**: T1.5.04
- **Produces**: `skill/celestial-blueprint/SKILL.md` (appends the final section)

## Context
The skill must work in three environments with graceful degradation: (1) **MCP mode** — the Celestial backend API is reachable, persist through it; (2) **standalone-with-tools** — no API but the agent can read/write files and web-search; (3) **bare** — only conversation. It also names the agent roster the user has, so the skill can suggest delegating research/critique. This is the last `SKILL.md` section.

## Objective
Append the degradation-modes + agent-roster section to `SKILL.md`.

## Instructions
1. APPEND to `skill/celestial-blueprint/SKILL.md` exactly:
   ```markdown
   ## Operating modes (graceful degradation)

   Detect what's available and degrade in this order:

   1. **MCP / API mode** — the Celestial backend is reachable
      (`GET /api/health` ok, or the `celestial-blueprint` MCP tools are present).
      Persist everything through the API: create blueprint, add nodes/
      assumptions/decisions/risks, post chronicles. The canvas updates live.
   2. **Standalone + tools** — no API, but you can read/write files and search.
      Write the blueprint markdown directly under `blueprints/` and chronicles
      under `chronicles/`, following `docs/FORMAT.md`. The user can later
      `POST /api/rebuild` to index them.
   3. **Bare conversation** — no tools. Produce the full blueprint markdown in
      a code block for the user to save. Still run the interrogation first.

   Never block on a missing tool: fall to the next mode and tell the user which
   mode you're in.

   ## Agent roster (delegation)

   When deeper work helps and the environment supports sub-agents/CLIs, delegate:

   | Need | Delegate to | Note |
   |------|-------------|------|
   | Heavy research | a web/research agent or Firecrawl | feed findings back as `research` nodes |
   | Adversarial critique | a "red-team" pass (opus) | challenge the 3 assumptions + 3 loopholes |
   | Large-tier synthesis | opus | 40+ node blueprints |
   | Quick gut-checks | haiku | small tier |

   If no sub-agents exist, do these passes yourself in sequence. Record any
   delegation as a chronicle (trigger: research changed direction).

   ## Definition of done

   A blueprint is "done for now" when: interrogation ran, the tier's output
   shape is met, every claim of fact is a `research` node or flagged as an
   assumption, the top 3 assumptions have falsifiers, risks are registered, and
   a CYCLE chronicle closes the session with a single next action.
   ```

## Acceptance Criteria
- [ ] `SKILL.md` contains "Operating modes" with all three degradation tiers
- [ ] The agent-roster delegation table is present
- [ ] A "Definition of done" closes the skill
- [ ] The full `SKILL.md` now has Step 0 through Definition of done in order

## Verify Command
```bash
cd /home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Blueprint
F=skill/celestial-blueprint/SKILL.md
grep -q "Operating modes" $F && grep -q "Agent roster" $F && grep -q "Definition of done" $F && grep -q "Select the tier" $F && echo "skill-05 ok"
```
Expected: prints `skill-05 ok`

## Done When
`SKILL.md` is complete: tier selection → interrogation → chronicle → output → degradation → done.
