# TASK T1.1.01 — Create full directory structure

- **Stage / Phase**: Stage 1 — Forge / Phase 1.1
- **Depends on**: none
- **Produces**: directory tree under the project root (+ `.gitkeep` files)

## Context
Celestial Blueprint is a web app with a Python backend and a React frontend, plus folders for the agent skill, docs, and blueprint/chronicle data. Some folders may already exist; this task guarantees the complete tree exists so later tasks have somewhere to write files.

## Objective
Create the complete project directory tree and add `.gitkeep` to otherwise-empty data folders.

## Instructions
1. Set `ROOT=/home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Strategist_Realm/Celestial_Blueprint`.
2. Create these directories (use `mkdir -p`, safe if they already exist):
   ```
   $ROOT/backend/app/api
   $ROOT/backend/app/core
   $ROOT/backend/app/models
   $ROOT/backend/app/services
   $ROOT/backend/migrations
   $ROOT/backend/tests
   $ROOT/frontend/src/components/canvas
   $ROOT/frontend/src/components/nodes
   $ROOT/frontend/src/components/chronicle
   $ROOT/frontend/src/pages
   $ROOT/frontend/src/hooks
   $ROOT/frontend/src/lib
   $ROOT/frontend/src/types
   $ROOT/docs/templates
   $ROOT/docs/prompt-packs
   $ROOT/skill/celestial-blueprint/reference
   $ROOT/blueprints
   $ROOT/chronicles/raw
   $ROOT/chronicles/cycles
   ```
3. Add a `.gitkeep` file to: `$ROOT/blueprints`, `$ROOT/chronicles/raw`, `$ROOT/chronicles/cycles`, `$ROOT/backend/migrations`.

## Acceptance Criteria
- [ ] All directories above exist
- [ ] `.gitkeep` exists in the four data folders

## Verify Command
```bash
ROOT=/home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Blueprint
test -d $ROOT/backend/app/api && test -d $ROOT/frontend/src/components/nodes && test -d $ROOT/skill/celestial-blueprint/reference && test -f $ROOT/blueprints/.gitkeep && echo "STRUCTURE OK"
```
Expected: prints `STRUCTURE OK`

## Done When
The full directory tree exists and data folders are git-trackable.
