# TASK T2.5.03 — Stage 2 end-to-end verification

- **Stage / Phase**: Stage 2 — Summoning / Phase 2.5
- **Depends on**: T2.5.02
- **Produces**: `BUILD_PLAN/STAGE_2_SUMMONING/VERIFY.md`

## Context

Stage 2 is complete when the agent scanner works, the registry API is live, routing resolves
correctly, and the frontend Agents tab displays the registry. This task creates the
verification checklist (parallel to `BUILD_PLAN/STAGE_1_FORGE/VERIFY.md`) and walks through
each check.

## Objective

Create `BUILD_PLAN/STAGE_2_SUMMONING/VERIFY.md` with the E2E checklist, then run each check
and confirm pass.

## Instructions

Create `BUILD_PLAN/STAGE_2_SUMMONING/VERIFY.md` with this content:

```markdown
# Stage 2 — Summoning: Verification Checklist

Run each command from the directory specified. The backend venv activates with
`source .venv/bin/activate` (sh/bash). All must pass before Stage 3 (Hands) begins.

## 1. Scanner produces discovered.json

```bash
cd backend && source .venv/bin/activate 2>/dev/null
python -m agents.scanner
python -m json.tool < agents/discovered.json | head -20
```

PASS: exits 0, prints "Discovered N agent(s)", JSON valid, at least 1 entry.

## 2. Agents table exists (init_db + init_agents)

```bash
cd backend && source .venv/bin/activate 2>/dev/null
CELESTIAL_DB_PATH=/tmp/celestial_v2_2.db python -c "
from app.core.db import init_db, init_agents, connection
init_db(); init_agents()
with connection() as c:
    cols = [r['name'] for r in c.execute('PRAGMA table_info(agents)')]
assert 'id' in cols and 'capabilities' in cols, cols
print('PASS')
"
rm -f /tmp/celestial_v2_2.db*
```

PASS: prints `PASS`.

## 3. Agent CRUD round-trip

```bash
cd backend && source .venv/bin/activate 2>/dev/null
CELESTIAL_DB_PATH=/tmp/celestial_v2_3.db python -c "
from app.core.db import init_db, init_agents
from app.models.schemas import AgentCreate, AgentPatch
from app.services.agent_store import upsert_agent, get_agent, patch_agent, delete_agent
init_db(); init_agents()
a = upsert_agent(AgentCreate(id='verify-agent', name='Verify', capabilities=['plan'], activation_cmd='verify'))
assert a.id == 'verify-agent'
patched = patch_agent('verify-agent', AgentPatch(status='active'))
assert patched.status == 'active'
assert delete_agent('verify-agent')
assert get_agent('verify-agent') is None
print('PASS')
"
rm -f /tmp/celestial_v2_3.db*
```

PASS: prints `PASS`.

## 4. Scan endpoint populates registry

```bash
cd backend && source .venv/bin/activate 2>/dev/null
CELESTIAL_DB_PATH=/tmp/celestial_v2_4.db python -c "
from fastapi.testclient import TestClient
from app.main import app
with TestClient(app) as c:
    r = c.post('/api/agents/scan')
    assert r.status_code == 200, r.text
    ids = [a['id'] for a in r.json()]
assert ids, 'no agents discovered'
print('PASS —', ids)
"
rm -f /tmp/celestial_v2_4.db*
```

PASS: prints `PASS — [...]` with at least one id (e.g. `claude-code`).

## 5. Route endpoint resolves code_gen

```bash
cd backend && source .venv/bin/activate 2>/dev/null
CELESTIAL_DB_PATH=/tmp/celestial_v2_5.db python -c "
from fastapi.testclient import TestClient
from app.main import app
with TestClient(app) as c:
    c.post('/api/agents/scan')                    # seed registry
    r = c.post('/api/route', json={'task_type': 'code_gen'})
    assert r.status_code == 200, r.text
    body = r.json()
assert {'agent_id', 'activation_cmd'} <= body.keys(), body
print('PASS —', body['agent_id'])
"
rm -f /tmp/celestial_v2_5.db*
```

PASS: response contains `agent_id` and `activation_cmd`; prints `PASS — <agent_id>`.

## 6. Frontend Agents page renders

1. Start backend: `cd backend && uvicorn app.main:app --port 8787`
2. Start frontend: `cd frontend && npm run dev`
3. Open `http://localhost:5173/` and click the "Agents" link (or go to `/agents`).
4. Click Rescan.

PASS: agent cards appear with name, status label (emerald=active / amber=inactive /
grey=unknown), and capability chips. No console errors.

## Stage 2 done when

All 6 checks pass. Proceed to Stage 3 decomposition.
```

After creating the file, run checks 1–5 (automated) to confirm they pass on the live
implementation. Record any failures.

## Acceptance Criteria

- [ ] `VERIFY.md` created at `BUILD_PLAN/STAGE_2_SUMMONING/VERIFY.md`
- [ ] Checks 1–5 pass on the implementation
- [ ] Check 6 passes manually in the browser

## Verify Command

```bash
ls /home/void-yggdrasil/CHAOS_DAO_FORGE/00_CORTEX/01_CREATION_REALM/Celestial_Blueprint/BUILD_PLAN/STAGE_2_SUMMONING/VERIFY.md
```

Expected: file exists (no error).

## Done When

`VERIFY.md` exists and all automated checks in it pass.
